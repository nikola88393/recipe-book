name = "recipe-book-proxy"
main = "src/index.ts"
compatibility_date = "2024-01-01"

[build]
command = ""

# AUTH_SECRET_KEY is set as an encrypted Wrangler secret — NOT stored here.
# Run: npx wrangler secret put AUTH_SECRET_KEY
# Then enter the same value you set as CLOUDFLARE_WORKER_SECRET in server/.env
